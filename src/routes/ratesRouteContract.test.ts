import assert from'node:assert/strict';import{readFileSync}from'node:fs';const page=readFileSync(new URL('../pages/CommercialPage.tsx',import.meta.url),'utf8');assert.ok(page.includes("view:tab.id")&&page.includes("requestedRateId={params.get('rateId')}"));assert.ok(page.includes("action')==='new-buy-rate")&&page.includes("action')==='new-sell-rate"));console.log('F8 route contracts PASS');
