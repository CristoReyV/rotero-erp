import { useState, type FormEvent } from 'react';
import { logClaimContact } from '@/services/claims.service';
import type { Claim360 } from '@/types/claims';

export function ClaimCommunicationPanel({claim,onChanged}:{claim:Claim360;onChanged:()=>Promise<void>}){const[contactId,setContactId]=useState(claim.available_contacts[0]?.id??''),[channel,setChannel]=useState('email'),[summary,setSummary]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState('');const contact=claim.available_contacts.find(item=>item.id===contactId);const submit=async(event:FormEvent)=>{event.preventDefault();if(!contact)return;setBusy(true);try{await logClaimContact(claim.id,{party:contact.party,contact_id:contact.id,channel,summary});setSummary('');setError('');await onChanged();}catch(e){setError(e instanceof Error?e.message:'No fue posible registrar el contacto.');}finally{setBusy(false);}};return <div className="space-y-3"><p className="text-xs text-slate-500">Bitácora estructurada; ROTERO no envía mensajes externos.</p><form onSubmit={e=>void submit(e)} className="grid gap-2 rounded-xl border bg-slate-50 p-3 md:grid-cols-[1fr_140px_2fr_auto]"><select required value={contactId} onChange={e=>setContactId(e.target.value)} className="rounded-lg border p-2 text-xs"><option value="">Contacto estructurado</option>{claim.available_contacts.map(item=><option key={item.id} value={item.id}>{item.party} · {item.name}</option>)}</select><select value={channel} onChange={e=>setChannel(e.target.value)} className="rounded-lg border p-2 text-xs"><option value="email">Email</option><option value="phone">Teléfono</option><option value="meeting">Reunión</option><option value="whatsapp">WhatsApp</option><option value="other">Otro</option></select><input required value={summary} onChange={e=>setSummary(e.target.value)} placeholder="Resumen del contacto" className="rounded-lg border p-2 text-xs"/><button disabled={busy||!contactId} className="rounded-lg bg-primary px-3 text-xs font-black text-white">Registrar</button></form>{error&&<p className="text-xs text-red-700">{error}</p>}{claim.contacts.map(item=><div key={item.id} className="rounded-xl border p-3"><b className="text-xs">{item.party} · {item.contact_name} · {item.channel}</b><p className="text-xs text-slate-600">{item.summary}</p></div>)}</div>}
