import type { FinanceProfitability } from '@/types/finance';
import { money } from './financeUi';
export function ProfitabilityWorkspace({ data }: { data: FinanceProfitability | null }) { if(!data)return <div className="rounded-2xl border bg-white p-12 text-center text-sm text-slate-400">Cargando rentabilidad…</div>; return <section className="overflow-hidden rounded-2xl border bg-white"><header className="border-b p-5"><h2 className="font-black text-slate-800">Rentabilidad operativa</h2><p className="text-xs text-slate-400">Esperado vs registrado vs caja. Cada moneda permanece separada.</p></header><div className="overflow-x-auto"><table className="w-full min-w-[1050px] text-left text-xs"><thead className="bg-slate-50 text-[10px] uppercase tracking-wider text-slate-400"><tr><th className="px-4 py-3">Operación</th><th>Esperado venta</th><th>Esperado costo</th><th>Registrado AR</th><th>Registrado AP</th><th>Caja neta</th><th>Saldo AR</th><th>Saldo AP</th></tr></thead><tbody className="divide-y">{data.operations.map((item)=><tr key={item.operation_id}><td className="px-4 py-4"><strong>{item.reference_code}</strong><p className="text-[10px] text-slate-400">{item.currency}</p></td><td>{money(item.expected_revenue,item.currency)}</td><td>{money(item.expected_cost,item.currency)}</td><td>{money(item.registered_ar,item.currency)}</td><td>{money(item.registered_ap,item.currency)}</td><td className={item.cash_margin>=0?'font-bold text-emerald-700':'font-bold text-rose-700'}>{money(item.cash_margin,item.currency)}</td><td>{money(item.outstanding_ar,item.currency)}</td><td>{money(item.outstanding_ap,item.currency)}</td></tr>)}</tbody></table></div></section>; }
